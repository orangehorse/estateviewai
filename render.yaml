services:
  - type: web
    name: estateviewai
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false
    healthCheckPath: /health
